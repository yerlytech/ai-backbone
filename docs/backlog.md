# Backlog

Notes for the backbone from the projects built on it: bugs, gaps, ideas.
`just backbone-note "one line"` in any project adds one. An agent working in
the backbone works through the open lines and ticks them (backbone-dev skill).

- [x] 2026-09-14, ai-backbone: setup.sh ends with "just is installed but not on the PATH yet" even when the install failed outright (just.systems unreachable). It should say the install failed and how to finish by hand.
- [x] 2026-09-14, Yerly XL: docs/README.md template references files that do not exist in a fresh project (01-getting-started.md, examples/, ADR 0001-0002) and its table row order is broken by an inserted paragraph; either ship those files or drop the rows — checked 2026-09-15, nothing to fix: the template is `.ai-backbone/seed/docs-README.md` and it names only `specs/` and `adr/`. A throwaway `just new-project` confirms it. The page that does name those files is `docs/README.md` in the backbone itself, which is the backbone's own documentation and says so on its second line. A project that still carries the old page predates 3.0.0; seeds are copied once and never updated, so its agent can simply rewrite it.
- [x] 2026-09-15, Yerly XL: stack rust: the pre-commit hooks do not run cargo fmt --check or clippy, so just save commits code that fails just lint; xlarge committed a clippy failure on 2026-09-15 this way — done in 3.7.0: one hook calls `just lint` before every commit, added by new-project, adopt and stack. A project that already has stack.just picks it up by running `just stack <lang>` again.
- [x] 2026-09-16, ai-backbone: stack-swift.just has no `lint` recipe, so the lint hook added in 3.7.0 stays quiet in a Swift project. Give the Swift layer a lint (swift-format --lint, or swiftlint when the project has it) so the hook has something to run. — done in 3.8.0: `swift format lint --strict` over Sources and Tests, plus swiftlint where a .swiftlint.yml asks for it; tried both ways on a throwaway package.
- [x] 2026-09-16, Yerly XL: audit skill: add a rehearsal step with concurrency on real services and keep rehearsals as recipes; after a 12-lens audit, running two servers with 200 saves and 50 live listeners found a driver session race and a live-query leak no lens saw — done in 3.8.0: the skill ends at a rehearsal and names four that have each found something real, each written as `just rehearse <name>` and each counting something.
- [x] 2026-09-16, Yerly XL: audit lenses template, second day: check that the deploy image's toolchain is at least every dependency's rust-version (cargo metadata); the Dockerfile said Rust 1.90 while a dependency needed 1.94 and no lens or test ran the image — done in 3.8.0: the second-day lens asks it.
- [x] 2026-09-16, Yerly XL: nothing reads two rules together and asks whether they agree; sync-rules keeps copies identical and self-test checks shape, but a September decision can quietly reverse a March one. — done in 3.9.0: a fifth always-there audit lens (the rules against each other) and a Relates to line in the ADR template.
- [x] 2026-09-16, Yerly XL: a project's agent has no way to know that the language, the database or the protocol it is built on has moved, or what changed in it; the maintainer was browsing GitHub by hand. — done in 3.10.0: docs/upstream.toml, just upstream, and the weekly line in session-start.
- [x] 2026-09-16, ai-backbone: routine.sh's lock outlives a run that dies outright (power, kill -9), and every later run says "already going" and does nothing. — done in 3.13.1: the lock holds the run's process number and a dead one is cleared; self-test plants both kinds of stale lock and runs two at once.
- [x] 2026-09-16, ai-backbone: self-test's "the routine refuses a time it cannot read" never reaches the time check. The note blamed the agent probe; a reviewer found it exits one step earlier still, at the missing brief, so no probe and no live agent call ever happened in the self-test. — done in 3.14.0: routine-install reads and checks the day and the time first, before the brief and before the probe, so the check reaches what it claims to check and the Linux cron line is the one that was asked for.
- [x] 2026-09-16, Yerly XL: A project with CI has no way to ask how the last push went. Add a recipe: 'just ci' shows the latest run's jobs and, when one failed, the failing step's last lines. Today that is 'gh run view --log-failed', which nobody remembers. Agent-tool independent: it is a just recipe, not a skill. — done in 3.16.0: `just ci` names every job of every run for the commit you are on and, for a job that failed, the last lines before GitHub's own ##[error] marker — 19 lines out of the 2667 `gh run view --log-failed` returns. The step names come from the API, not the log, because gh sometimes labels every line of a job UNKNOWN STEP. The extraction is its own recipe (`_ci-lines`) so the self-test can feed it a real failed job's log and check what comes out.
- [x] 2026-09-16, Yerly XL: The backbone ships no CI starter, so every project relearns the same four lessons the hard way. Add docs/examples/ci.yml (and a 'just ci-init'): free the runner's ~20GB of unused toolchains before a big build; debug=line-tables-only in CI, because a linker out of disk dies with 'Bus error' and names no cause; --no-fail-fast, or one failure hides the rest; a concurrency group that cancels on PRs but gives every push on the default branch its own group; and never prove a binary's contents with 'strings' when the profile strips it — read cargo's own --message-format=json record instead. All four cost Yerly XL a day of red builds on 2026-09-16. — done in 3.16.0: `.ai-backbone/examples/ci-rust.yml` and `ci-generic.yml`, written by `just ci-init` together with `.github/dependabot.yml`. The note said `docs/examples/` because the seed AGENTS.md says so (section 6); that line is a [seed] file no update corrects, and every example has lived in `.ai-backbone/examples/` since 3.0.0. Four of the five lessons are settings rather than steps, so `just ci-check` fails a workflow that has dropped one, on the commit.
- [x] 2026-09-16, ai-backbone: there is no ci-swift.yml. What is known and unverified: the runner must be a named macos-NN, not macos-latest, which flips the Xcode under you with no commit; Xcode is pinned with xcode-select, which is Swift's dtolnay/rust-toolchain; the disk lesson does not transfer, because those paths are Ubuntu's and the bulk on a Mac runner is Xcode, which cannot go; there is no Swatinem/rust-cache equivalent, only actions/cache keyed on Package.resolved. Measure one real macOS run before shipping a starter, and until then `just ci-init swift` offers the generic one. — still open on 2026-09-19: no macOS run has been measured yet, and 3.20.0 measured none. Same project: add `.build/checkouts/<package>/` to `where()` in upstream.py and to the session skill, once a Swift checkout has been looked at. — blocked: one real macOS run measured, and one real Swift project — re-checked 2026-09-23 (spec 015): still blocked. No Swift project on the maintainer's machine, and GitHub Actions is off on this repository (the API answers enabled: false), so there is no macOS run to measure. — closed 2026-09-23: it waits for GitHub Actions to be switched on, which is the maintainer's decision, taken when the time comes; not kept open as a note. What was learned stays on this line for whoever builds it then.
- [x] 2026-09-16, ai-backbone: `cmd | grep -q x` is a race in the self-test: grep leaves as soon as it matches and a command still writing gets SIGPIPE, which pipefail turns into a failure. It cost a fake FAIL in 3.16.0 and it is latent in about five older checks. Either wrap them all as `{ cmd 2>&1 || true; } | grep -q`, or give the suite a `says` helper that captures first and greps a variable. — done in 3.17.1: the `says` helper, and every check converted, thirty-odd of them rather than five. The race is worse than a fake FAIL: in a `! cmd | grep -q x` check the SIGPIPE makes the pipeline fail exactly when the text *is* found, so the check passes on the failure it was written to catch. Two were doing that — `just doctor` was counting a machine without `gh` as a broken project, and `_has-code` was counting `.ai-backbone/upstream.py` as the project's code and telling every new project to map code it had not written. A check of the suite against itself fails if a pipe into grep comes back.
- [x] 2026-09-16, Yerly XL: just ci-check's waiver is per file, not per line: '# ci-check: skip no-fail-fast' switches the rule off for the whole workflow, so waiving one deliberate line also stops it catching a real regression on another. Yerly XL hit this on 2026-09-16 with a second, single-binary cargo test line; the remedy happened to be free, but the next project may not be so lucky. Make the waiver attach to the line above it, or name what it waives: '# ci-check: skip no-fail-fast on this line'. — done in 3.19.0: a `# ci-check: skip no-fail-fast` comment written on a `cargo test` command, or on the line directly above one, waives that line alone; written anywhere else it still waives the file, so no project's existing waiver changes meaning. The refusal names the line number and offers the narrower form.
- [x] 2026-09-17, Yerly XL: publish-on-save is on: a push is a public act, so save should run the project's own check before pushing and keep the commit local when it fails — today a red lint reaches GitHub and CI emails the maintainer — checked 2026-09-19, nothing to fix: the lint half closed in 3.7.0 and 3.18.0 — the hook runs `just lint` inside the commit, `save` pushes only after the commit went through, and `just doctor` warns when a project has a lint and nothing runs it. The test half is the project's: only the project knows which of its tests are cheap enough for every save, and Yerly XL answered it the same day with a `just test-fast` hook before the commit, while spec 009 keeps a commit cheap for everyone else; 3.20.0 ships `test-fast` in the Rust example. A gate before the push would not keep a red commit local anyway: the next green save pushes HEAD and every commit under it.
- [x] 2026-09-18, ai-backbone: just upstream says 'Everything watched is current.' when it could not read a single source. .ai-backbone/upstream.py line 224 builds the blind list from the 'how' column (if how in ('unknown','unreachable')), but line 183 puts 'unreachable' in the 'newest' column and the error text in 'how' — so an unreachable source is never counted blind and the weekly report claims everything is current. Seen on 2026-09-18: all four watched tools 403 behind a sandbox proxy, and the last line still said everything was current. Test it with a watch list whose source cannot be reached. — done in 3.19.1: the reason a source could not be read now travels beside the table, not in the column the summary reads, so an unreachable source is counted blind, named under UNKNOWN with what came back, and a run that read nothing says so instead of 'Everything watched is current.'. Guarded by a self-test with a watch list whose source cannot be reached, offline.
- [x] 2026-09-18, ai-backbone: sh .ai-backbone/setup.sh -y ended with 'just could not be installed' on a fresh Linux sandbox, and the very next 'uv tool install rust-just' by hand worked. So the install is reachable and something about the run is not: a transient proxy failure, or uv not yet on the PATH at line 38. setup.sh swallows nothing, but it also never retries and its last word sends the reader off to type the line that just failed. Consider one retry, and printing what uv actually said. — done in 3.20.0: each uv install is asked twice, the floor's move included, both attempts on the screen, so uv's own words are the reason; the ending says just is not on the PATH and points at them instead of a command to retype; the unreachable "not on the PATH yet" branch is gone. The self-test runs setup.sh with every tool faked, under dash. The run's own output was never recorded, so which of the two suspects it was stays unknown; next time the screen will say.
- [x] 2026-09-18, Yerly XL: just save and just backbone-note splice their text argument into the recipe's bash script unquoted: a dollar sign followed by a word fails with 'unbound variable', and a dollar-paren command substitution would run. Pass the text as a positional argument or environment variable, never as a just interpolation inside the script body. — done in 3.20.0: every recipe that takes a parameter — twenty-one in core.just, the Swift example's build-app — carries `[positional-arguments]` and reads `$1`; a project's name reaches perl through the environment; a self-test guard lists any recipe that goes back to `{{param}}`, a quiet `@name arg:` included. A global `set positional-arguments` was rejected: it reaches the project's own recipes and is a hard error when the project sets it too. Needs just 1.29, which setup.sh now checks and moves.
- [x] 2026-09-19, ai-backbone: setup.sh exits 0 when just could not be installed at all and 1 when it is merely too old, so a caller reading the exit code gets opposite answers for the worse case. The backbone's CI swallows both with `|| true`. Decide one meaning and say it in the header. — done in 3.21.1: the code answers one question, "are the tools here", and the header says so: 0 when git, just (1.29.0 or newer), uv, prek and graphify are all on the PATH as the script ends, 1 when any of them is not, and the last lines name what is missing. `gh` is left out of the count on purpose (only `just publish` needs it) and the repo's own state stays `just doctor`'s, which is why this repo's CI still names the tool it wants. Two self-test checks hold both ends.
- [x] 2026-09-19, ai-backbone: `just ci` prints "The job never started. GitHub's reason: …" for a failed job with no failed step, no log and a failure annotation. A job that started and lost its runner may look the same. Not reproduced; needs one real lost-runner run to read. — blocked: one real run that lost its runner, to read — re-checked 2026-09-23 (spec 015): still blocked. Actions is off on this repository (enabled: false), so no run can start, let alone lose its runner. — closed 2026-09-23: it waits for GitHub Actions to be switched on, the maintainer's decision, taken when the time comes. What was seen stays on this line.
- [x] 2026-09-19, ai-backbone: `just save` refuses a linked git worktree with "No git repo yet. Run: git init" — `.git` is a file there and the recipe asks `[ -d .git ]`. Ask git instead (`git rev-parse --git-dir`), in save and wherever else the same test stands. — done in 3.23.0 (spec 013): save, ci and template-update now ask git through a new hidden helper, _is-repo, and work in a linked worktree (tried by hand and in the suite; prek 0.5.3 run from a worktree installs into the shared hooks folder, measured). The helper asks `git rev-parse --show-cdup` (empty at the top of a work tree, linked or not) and NOT `--git-dir` as the line suggests: --git-dir also succeeds in a plain folder inside somebody else's repository, and from there the `git add -A` of a save stages that whole repository (measured on git 2.55.0). The other places the same test stands (doctor x3, session-start x3, adopt x1) are not mine: exact old and new text is under needs_elsewhere, and was verified in a scratch clone (suite 219 ok, doctor reads right in a linked worktree).
- [x] 2026-09-19, ai-backbone: routine.md step 6 skips a backlog line that an approved, unfinished spec says it closes. Nothing says when a claim lapses: a spec abandoned in practice but still marked approved holds its lines for ever. One sentence, perhaps an age. — done in 3.21.0 (spec 011): a claim lapses when the spec's file has not changed for 14 days, measured with git log after an unshallow where the checkout is shallow.
- [x] 2026-09-19, ai-backbone: the ADR template's Measured-on line accepts any reason after "Nothing"; if a project's ADR ever reads "Nothing, from memory", tighten the last sentence. — done in 3.23.0 (spec 013): Tightened without waiting for the slip (no ADR in the maintainer's projects reads "Nothing, from memory" yet; their docs/adr folders were read, not written, on 2026-09-19): "Nothing" is allowed only when the decision says nothing about what a tool or a version does, and then says that; "From memory" is never the reason: a claim that was not measured is measured first, or taken out. .ai-backbone/templates/adr.md, one sentence.
- [x] 2026-09-19, Yerly XL: a Flutter project has no ready language layer or CI starter (only rust and swift). Write `stack-flutter.just` and `ci-flutter.yml` from the first real Flutter project, not before: the Rust example was only right after a real project rewrote it. (An earlier sentence here said graphify does not parse Dart; it has since 0.4.10, see the line about `_has-code` below.) — blocked: the first real Flutter project's first spec has not passed its acceptance list (it was a draft on 2026-09-21). What did not depend on the lift went in with 3.23.0: Dart counts as code, the seed files, the update bot, the audit lens, the code map. — done in 3.25.0 (spec 015): `examples/stack-flutter.just` and `examples/ci-flutter.yml`, lifted from the first Flutter project on this backbone, whose spec 001 had passed every technical acceptance line (the open one is the owner's verdict after playing) and whose specs 003–005 were done, so the event this waited for had happened.
- [x] 2026-09-19, ai-backbone: just ci says "The checks have never run here. They start on the next push" in a repository whose Actions are turned off on GitHub (Settings, Actions, Disable actions), where they will never start. gh api repos/{owner}/{repo}/actions/permissions answers enabled=false: say that instead, and that it is switched back on in the same place. Seen 2026-09-19, after the maintainer turned Actions off because a private repository out of free minutes mailed a failure on every push, and the cloud routine pushes daily. — done in 3.23.0 (spec 013): When the commit has no run, just ci first asks gh api repos/{owner}/{repo}/actions/permissions; on a plain `false` it says the checks are switched off and prints <repository>/settings/actions, where they are switched back on. Measured read-only on 2026-09-19 with gh 2.101.0 against the maintainer's repositories: with Actions off the run list answers [] (ai-backbone) or its old runs (xlarge), where ci called a pushed commit "not reached GitHub"; both cases now read right, tried against the real repositories from a throwaway checkout. Somebody else's repository answers 403 and reads as before; a green or red build costs no extra call. Found next to it and fixed: just publish said "The checks start now" in the same repositories; it asks the same question and says nothing there.
- [x] 2026-09-19, Swap Smash: just upstream says "current" for github:flutter/flutter whatever has come out: its GitHub releases stop at 3.19.0-0.1.pre, and the tag fallback runs only when there are no releases at all (and /tags?per_page=30 returns v1.x). A pin of 3.44.0 printed "current, nothing newer" while 3.47.2 to 3.47.5 existed. Fix: when the source lists neither the pin nor anything newer, the row is unknown and gives a reason (compare with parts(), not as strings). Add a flutter:<channel> source that reads storage.googleapis.com/flutter_infra_release/releases/releases_<os>.json (dedupe the x64 and arm64 rows, date from release_date). where() for a github source should also find the SDK's git checkout and show its CHANGELOG and its tag against the pin. Match the origin URL, because /opt/homebrew is itself a git repo. — done in 3.23.0 (spec 013): all three parts. (a) A list that holds neither the pin nor anything newer makes the row `unknown`, never `current`, and the reason stands under the table: the newest version the source does list, or, when the pin is written another way than the source writes its versions, what they look like. Compared with shape()/parts(), never as strings. (b) New source kind `flutter:stable` (and `flutter:beta`) reads storage.googleapis.com/flutter_infra_release/releases/releases_<os>.json: one row per version (a Mac's file has an x64 and an arm64 row, and 3.13.3 was built three times over five days), dated by the first day it existed; on the beta channel a beta is the release, so no `prereleases = true` is needed; a channel that is not there is said with the ones that are. Tested offline against a fixture cut from the real file of 2026-09-19. (c) `just upstream <name>` lists the …
- [x] 2026-09-19, Swap Smash: _has-code has no .dart, but graphify has parsed Dart since 0.4.10 (measured on 0.9.64: 74 nodes from swap-smash's lib/ and test/). The open line "graphify does not parse Dart" is wrong, and so is the SQL wording in update-map and _has-code. Add only *.dart: syncing graphify's whole CODE_EXTENSIONS would pull in .json and .sh and make every new project "have code". Also fix getting-started's language list and add a self-test for a Dart-only tree. swap-smash says yes today only because of its Swift and Kotlin runner shims. — done in 3.23.0 (spec 013): _has-code counts *.dart and nothing else new (graphify 0.9.64's own list has 102 extensions with .json and .sh among them, which every new project has). The SQL wording in _has-code's comment and in update-map's sentence is corrected from a measurement on 0.9.64: .sql IS on graphify's list, but its parser is an extra (graphifyy[sql]) that `uv tool install graphifyy` does not bring, so a tree of SQL alone ends in "graph is empty", exit 1; HTML is a document that --code-only skips, CSS is not read at all. A Dart-only tree has a self-test check. What is left is not mine: the language list in docs/01-getting-started.md (the new wording is under docs_to_update). I left "since 0.4.10" out of the comment because nothing on this machine could confirm it.
- [x] 2026-09-19, Swap Smash: The code map is mostly backbone plumbing, in every language. swap-smash's first update-map gave 102 nodes: 50 came from .ai-backbone/upstream.py, setup.sh and routine.sh, and 6 from the game. The second run (graphify update .) pulled in 22 .md files and made Changelog the top node. Add .ai-backbone/ and *.md to templates/graphifyignore. Append missing template lines to an existing .graphifyignore, as _hook-lint does for the hook config. Run graphify extract . --code-only every time: it is incremental (measured). Measure at the same time, on the pinned graphify (seen on 0.9.58, 2026-09-11): after files are deleted their nodes stay until the update is forced, and after the project folder moves the map needs a fresh extract, because the root is stored as an absolute path. `update-map` does neither. — done in 3.23.0 (spec 013): templates/graphifyignore names .ai-backbone/ and *.md; update-map adds the template lines an older .graphifyignore lacks, at the end, never moving or removing anything, and a line somebody commented out counts as answered (the project's way to say no); every run is now `graphify extract . --code-only`. Measured on graphify 0.9.64 in a scratch project with swap-smash's Dart files: old list gave 76 nodes, 52 from .ai-backbone/ and 6 from the game; `graphify update .` gave 217, 141 of them .md. extract --code-only is incremental and correct (second run re-read only two JSON settings files and wrote a byte-identical graph; an edit, a renamed class, a new file came out right). Deleted files: their nodes are pruned on the next run with NO --force, in extract and in update alike, so the 0.9.58 observation no longer holds. Newly ignored files are pruned the same way (217 -> …
- [x] 2026-09-19, Swap Smash: AGENTS.md §4 tells agents to run graphify affected before a change, but graphify 0.9.64 draws no call edges in Dart: "affected swap" found none of its 2 real callers. Add a language-neutral just uses <word> (git grep -w over the project, skipping .ai-backbone, brain, graphify-out, .references and .archive) and reword §4. AGENTS.md is a seed, so the CHANGELOG entry must name it. — done in 3.23.0 (spec 013): New recipe `just uses <word>` (group map): git grep, whole word, fixed string, saved and brand-new files, skipping .ai-backbone, brain, graphify-out, .references and .archive, lines cut at 240 bytes, ends with a count, exit 0 with a calm sentence when nothing names the word. Confirmed on 0.9.64: 12 edges among swap-smash's Dart nodes, none a call (Python in the same map: 33); graphify's Dart reader is regular expressions and its only `calls` edges are a BLoC's events and states. The ceiling: backbone-news is called by session-start only (no skill, rule or other recipe names it; docs/01-getting-started.md lists it once among commands), so it became _backbone-news and a new project still shows 37. Left for others: session-start's one calling line, the AGENTS.md section 4 sentence, and the getting-started lines (all under needs_elsewhere / docs_to_update). AGENTS.md is …
- [x] 2026-09-19, Swap Smash: doctor says "Everything needed is installed" on a Mac with no Flutter. After that, every save that touches code fails in the lint hook with "dart: command not found" (exit 127). Add a generic _stack-doctor hook that doctor calls when stack.just defines one, like _root-allow. End doctor three ways, so nobody is sent to setup.sh for a language SDK. Also, "rust | swift" is written by hand in five places (doctor's hint, stack, ci-init, the docs); derive the list from examples/stack-*.just. — done in 3.23.0 (spec 013): done: stack.just names what its language needs in a private _stack-doctor recipe (one line per tool: the command, then where to get it), the way _root-allow names root files; doctor lists them under "Language", counts them apart from the backbone's own five tools, and ends three ways, so a project that lacks only its SDK is never sent to setup.sh (both missing: the old ending plus one sentence saying setup.sh installs no language). The endings go through a private _doctor-msg shaped like _msg, so a chat_lang=tr project reads them in Turkish; the English is unchanged. "rust | swift" by hand: doctor's hint and stack now read the stack-*.just file names through a private _examples helper (ci-init's "Ready:" list uses it too), and `just stack` alone lists the ready layers. Not mine to edit and handed to the integrator with exact text: the three doc sentences (AGENTS.md …
- [x] 2026-09-19, Swap Smash: ci-init writes .github/dependabot.yml only when a ci-<lang>.yml exists, so dependabot-swift.yml is unreachable and a Flutter project gets no update bot. Decouple the two, and add dependabot-flutter.yml (pub plus github-actions), named for the language ci-init looks up. In a private repository, ci-init should say what a run costs: xlarge used its 2,000 free minutes in two days (09-16 and 09-17), and Actions is now off on xlarge and ai-backbone. — done in 3.23.0 (spec 013): done: ci-init writes the update bot first, for the language it finds, and the workflow when one is ready, so a Swift project gets dependabot-swift.yml and a Flutter one (found by pubspec.yaml, at the root or one folder down) gets the new dependabot-flutter.yml (pub plus github-actions; GitHub's options reference lists pub and says of enable-beta-ecosystems "Not currently in use", read 2026-09-19). With no ready workflow it still ends in "No ready checks ... just ci-init generic" and exit 1, after saying the bot was added. What a run costs is one sentence: private (gh says so) or not knowable (offline, no gh, no address yet: said of the kind just publish creates); silent only where gh answers public. No project is named. `just stack <lang>` points at ci-init when only a bot file exists.
- [x] 2026-09-19, Swap Smash: Seed files against a Flutter game. .editorconfig gives *.dart 4 spaces where dart format wants 2: add dart to the 2-space glob. The seed .gitignore has *.jks but not *.keystore, so a keystore outside src/android is committed by save's git add -A. It also needs a Flutter line: keep the nested .gitignores flutter create writes, add .kotlin/ to src/android, never *.xcodeproj in a Flutter app. check-json rejects the JSONC .vscode/extensions.json that VS Code writes. pin_is_real is a substring test, so 1.38.2 matches 1.38.20. templates/upstream.toml has no pub row saying that pinned_in is pubspec.lock. — done in 3.23.0 (spec 013): Everything but the two upstream.py sentences (pin_is_real, the pub row), which belong to the upstream package. .editorconfig gives *.dart 2 spaces (measured: dart format 3.13.4 writes 2); it is a [project] file, so template-update carries it to existing projects. The seed .gitignore ignores *.keystore and *.mobileprovision beside *.pem *.key *.p8 *.p12 *.pfx *.jks, with a comment that key files live in brain/, and its stack examples gain a Flutter line (.kotlin/, keep the .gitignore files flutter create writes, never *.xcodeproj; measured on Flutter 3.47.5: flutter create's android/.gitignore has no .kotlin/, and swap-smash's android/.kotlin/ exists after a build). check-json in the seed hook config now excludes .vscode/*.json as well as tsconfig: reproduced on prek 0.5.3 with the exact extensions.json template a VS Code build writes (read out of Cursor 3.16.29 on …
- [x] 2026-09-19, Swap Smash: Lift swap-smash's stack.just into stack-flutter.just once its spec 001 passes its acceptance list. swap-smash already has: every Dart folder in lint, and a check-versions that checks every assignment and says what it checked. Still to add on the lift: an app_dir for src/<part> that stops with a sentence, not a raw flutter error (lint must stay quiet when there is no pubspec yet, or save stops between just stack and flutter create); _root-allow plus .dart_tool/ and build/ for a pubspec at the root; the JDK lesson (doctor green, APK died in jlink, Flutter blamed AGP below 8.2.1 on AGP 9.1.0, JDK 21 via flutter config --jdk-dir fixed it); text-only self-test checks. ci-flutter.yml: ubuntu only, analyze and test (3-4 s each here). flutter-version-file needs an exact environment.flutter pin. — blocked: the first real Flutter project's first spec has not passed its acceptance list (it was a draft on 2026-09-21). What did not depend on the lift went in with 3.23.0: Dart counts as code, the seed files, the update bot, the audit lens, the code map. — done in 3.25.0 (spec 015): every Dart folder in lint, the app folder that stops with a sentence (and one app in src/<name> as a shape of its own), lint quiet with no pubspec.yaml yet, _root-allow with .dart_tool/ and build/ for a root pubspec, the JDK lesson on build-android, check-versions over every assignment, text-only self-test checks (no Flutter in the suite), and ci-flutter.yml on Ubuntu only.
- [x] 2026-09-19, Swap Smash: template-update reads the old version from the working copy, so a half-applied earlier update hides its span. duybu, randoms, solball and yerly-tech had 3.7.0 committed and 3.17.1 on disk, left there by the removed projects-update job, and the AGENTS.md notices between 3.7.0 and 3.17.1 were never shown. Read the version from HEAD (git show HEAD:.ai-backbone/core.just). Those four AGENTS.md files still lack the newer rules; a recipe that lists the seed rules a project's AGENTS.md lacks would help. — done in 3.23.0 (spec 013): template-update reads the version a project is on from HEAD (git show, captured and then read so no pipe breaks under pipefail), and from the file only where git has no answer. It ends, up to date or not, by listing the "- " rules of the backbone's Session protocol, Safety and Do not sections (3, 7, 8) that the project's AGENTS.md has no line for, compared by their first six words without case and punctuation; it says that a reworded or deliberately dropped rule is listed too, changes nothing, adds no recipe. Dry-run read-only on real projects: xlarge lacks four rules; a colon did not make yerly-tech's "Ask first before deleting" a missing one. The existing plant of 3.5.0 in the suite had to be committed (one line), because a disk-only plant is exactly what is no longer read.
- [x] 2026-09-19, Swap Smash: templates/audit-lenses.md: the second-day lens asks about a Docker image and rust-version. A Flutter app's toolchain mismatch lives elsewhere: the environment.sdk and environment.flutter constraint against CI, the iOS deployment target against plugins, and Android minSdk, AGP, Gradle, Kotlin and the JDK. Make the lens general, or give the Flutter layer a line of its own. — done in 3.23.0 (spec 013): The lens is general now: does whatever builds the app away from the development machine (CI, the image, the compose file, the store build, the install guide) install at least what the app and every dependency declare. Two examples follow it: the Rust 1.94 against Dockerfile 1.90 case, and the Flutter case (pubspec's environment: against CI's Flutter, the iOS deployment target against plugins, Android's minSdk, Gradle, its Android plugin, Kotlin and the JDK). No Flutter line of its own was added. A project that already has docs/audit-lenses.md keeps its copy.
- [x] 2026-09-19, ai-backbone: backbone-note never refreshes the clone before it appends, and pushes with a single try. When GitHub has moved (the cloud routine pushes every day) the push is refused, the message says "GitHub not reachable", and the clone is left ahead and behind: from then on _backbone-refresh (pull --ff-only) can never bring it level and says nothing, template-check compares every project against a stale tree, later notes pile up unsent, and just publish is refused too. Measured 2026-09-19 on scratch clones against a bare stand-in remote; the real clone diverged once that morning and was joined by hand. Wanted: refresh before appending; on a refused push let a fetch decide between "GitHub moved" and "not reachable", rebase once, abort on failure, and say what really happened; refresh with pull --rebase plus rebase --abort instead of --ff-only, and say why when it cannot (unsaved work, another branch, does not fit); self-test checks on a bare remote for a note written while behind and for a diverged clone. — done in 3.21.0 (spec 011): one helper, `_backbone-level`, used by the refresh, the note and the backbone's session-start: fetch, rebase, abort when it does not fit, send the notes that never went out, say why when it cannot; one session at a time; `docs/backlog.md` merges as a union. Eighteen checks on a bare stand-in remote.
- [x] 2026-09-19, ai-backbone: the backbone's own session-start prints "GitHub: up to date." in three different states: level, ahead with commits that were never sent, and after a pull --rebase that failed. In the third it leaves the repository in the middle of a rebase on a detached HEAD, and the next note from a project commits the conflict markers into docs/backlog.md, because the note's dirty check leaves that file out. Measured 2026-09-19 on scratch clones. Git prints the CONFLICT text on stdout, so 2>/dev/null does not hide it. The "not pulled" message also tells a person to type git pull --rebase, against AGENTS.md section 1. Wanted: abort a failed rebase, say "ahead" and "does not fit" in plain words, never "up to date" after a failure, and a self-test check that no .git/rebase-merge is left behind. — done in 3.21.0 (spec 011): the same helper: never 'up to date' after a failure, no rebase left behind, 'ahead' is said, and no message asks a person to type git.
- [x] 2026-09-19, ai-backbone: backbone-note can say "sent to GitHub" when nothing was sent. When a hook refuses the note's commit the && chain stops, but the push line still runs on an unchanged HEAD and succeeds, and the uncommitted line leaves the clone dirty so every later refresh is skipped without a word (measured 2026-09-19 with a stand-in hook). When the clone is on a branch other than main the note is pushed to that branch, where the routine never looks (read in core.just). A note whose text holds a newline also writes a second backlog line with any date and any author (measured on the recipe's own printf). Wanted: push only after the commit went through, say "sent" only when origin/main holds the commit, say so when the clone is not on main, and flatten newlines and control characters in the text. — done in 3.21.0 (spec 011): it pushes only after its commit went through, never the saved work under it, says so off main, refuses a clone that would lose the line, and writes one line whatever the text holds.
- [x] 2026-09-19, ai-backbone: routine.md still assumes a machine a person works on, while the scheduled run is a fresh checkout in a sandbox. Step 12 writes the run's report into a brain/ that vanishes with the sandbox, so every run starts with "no journal entries yet" and nothing a run learned reaches the next one, and "never touch brain/" stands beside two steps that write there. Step 0 writes a journal line and step 5 reports brew. Step 11 has no instruction for a refused push, although projects send notes to main at any hour. Step 4 relies on session-start, which prints only the newest 12 open lines while step 6 says oldest first. Lines that cannot be done unattended because they wait for a measurement are read again every day. The stored prompt of the scheduled run also paraphrases this file and ends the item search one step early, so the last resort of step 6 (borrowing from the references) has never been reachable. Read 2026-09-19 in the run log of the scheduled routine. — done in 3.21.0 (spec 011): the brief is written for a fresh checkout: one line per run in `docs/routine-log.md` instead of a journal, a step for a refused push, `blocked:` and `- [?]` lines, the whole file as the queue; the stored prompt points at the brief and carries the limits itself.
- [x] 2026-09-19, ai-backbone: self-test's check "ref-add adds the guide and the list" greps the literal word ai-backbone in .references/references.toml, and ref-add names the clone after the folder, so the whole suite goes red in a checkout whose folder has another name (a scratch copy, a second clone). Grep for the basename of $root instead. Four agents hit it on 2026-09-19 and none wrote it down. — done in 3.23.0 (spec 013): The check now looks for the name line with the basename of $root, lower-cased as ref-add does, and for the clone's folder. The whole suite is green from a second clone named Baska-Isim (219 ok). The old check could NOT be seen failing anywhere under my scratch folder: every path there contains the word ai-backbone (the session folder is named after the project), and the old grep matched the url line, which is a second fault of the old check. Shown instead with a list that holds no such word (ref-add of a relative path): old grep FAIL, new grep ok.
- [x] 2026-09-19, ai-backbone: the splice guard's awk in self-test.sh (`spliced`) was proven only under macOS awk 20200816. Ubuntu's default awk is mawk, and with GitHub Actions off the scheduled run's Linux sandbox is the only place that can measure it: run the guard there against a recipe that splices, write in the suite's comment which awk it was seen working under, and only then consider the one shape it misses, a parameter inside an expression, which no backbone recipe uses. — done in 3.22.0: measured with a one-off run in the scheduled agent's sandbox, where `awk` is mawk 1.3.4 20240123: nothing on the real recipes, every planted offence named; the suite's comment says so. The shape it does not see, a parameter inside an expression, is left: no recipe has it.
- [x] 2026-09-19, ai-backbone: setup.sh installs just on Linux as rust-just from PyPI (`uv tool install rust-just`), which is a third-party repackaging and not the author's own release; no tracked file says so, and docs/upstream.toml watches only github:casey/just. Say it in setup.sh's header and where docs/01-getting-started.md introduces just. A watch row for it would need a pypi: source kind upstream.py does not have: note that, do not build it. — done in 3.23.0 (spec 013): Said in setup.sh's header (comments only) and where docs/01-getting-started.md introduces just: on Linux just is rust-just from PyPI, a third party's repackaging (its repository is github.com/gnpaone/rust-just; read on PyPI 2026-09-19 at 1.58.0, level with the author's newest tag), used on purpose because it arrives where just.systems is unreachable. The header notes that docs/upstream.toml watches github:casey/just and that a row for the package would need a pypi: source kind upstream.py does not have. Not built.
- [x] 2026-09-19, ai-backbone: `just tools-update` upgrades graphify but never moves its pin in docs/upstream.toml: 0.9.62 was pinned and 0.9.64 installed on 2026-09-19. Either tools-update moves the pin when it upgrades, or the row says the pin is the oldest version known to work and is not meant to follow. — done in 3.23.0 (spec 013): tools-update now ends by moving the pin of just, prek, graphify, gh and uv in docs/upstream.toml to the installed version, in the same run, with one line per pin moved. It finds a row by its `source` and only touches a row with no `pinned_in`. It only moves a pin upwards, and rewrites only that one line (awk finds the line number, perl -pi substitutes on it). I chose "installed is newer than the pin" over "upgraded in this run", so the 0.9.62 / 0.9.64 drift that exists today heals at the next run. The final "Done. Review …" line names docs/upstream.toml when a pin moved.
- [x] 2026-09-19, ai-backbone: two limits were accepted on purpose and are written nowhere an agent reads, so a close reading will find them as bugs: the commit-message check only catches Turkish-specific letters, so Turkish written in plain ASCII passes; and gitleaks lets the documented AWS EXAMPLE key through by design, while realistic AWS, GitHub and Stripe keys were blocked when tried (2026-09-13). Write both as comments where the checks live. Comments only. — done in 3.23.0 (spec 013): Comments only, where the checks live. Above _commit-msg-check in core.just: the check tests for a letter outside ASCII (any, not only Turkish ones, which corrects the line), so Turkish typed in plain ASCII passes, "naive" with a diaeresis is refused, arrows pass (all three measured 2026-09-19), and why that is enough. Above the gitleaks hook in the root .pre-commit-config.yaml: the AWS rule's own allowlist lets whatever ends in EXAMPLE through (read in gitleaks' config at the pinned tag), a random AKIA key was refused on 2026-09-19. The comment carries no version number on purpose: upstream.py's pin_is_real looks for the pin anywhere in that file, and my first draft, which named it twice, would have hidden a moved pin; a check now holds the version to one occurrence. The just doc comment of the recipe is unchanged (checked with just --dump).
- [x] 2026-09-19, ai-backbone: upstream.py reads three shapes of tag wrongly, all measured on 2026-09-19 with its own parts() and newer_than(): a monorepo sub-package tag (python/x/v1.0) reads as newer than the pin v0.9; a date-shaped pin works by accident, and with prereleases on, 2026-07-28-RC reads as newer than 2026-07-28; a prefixed tag (rust-v0.155.1) yields no numbers, so it compares correctly only when the pin carries the same prefix. One change to how a tag becomes numbers, and a self-test line for each shape. — done in 3.23.0 (spec 013): one change, shape(): a tag is the label before its version plus the version, and newer_than() keeps to the pin's own label. python/x/v1.0 is not the next version of a pin of v0.9 (and is, of a pin of python/x/v0.9); 2026-07-28 is three numbers and -RC its suffix, so the RC sorts before the date; rust-v0.155.1 is [0,155,1] under the label rust-, and python-v9.0.0 or rusty-v8-v152.2.0 beside it are other things (all three shapes measured on google/A2UI, the MCP specification and openai/codex). Also: 1.0 equals 1.0.0, rc10 sorts after rc9 without the dot, and tags_with_git() is kept right in two ways: it keeps every tag instead of the fifty highest (sorted together, codex's newest rust-v release stood at place 49 of 1,358 and the pin fell off the end; 1,116 flutter tags list and compare in under a second), and it knows -rc1 and _RC1 for a prerelease where only -rc.1 was …
- [x] 2026-09-19, ai-backbone: session-start touches .git/ai-backbone.last-upstream even when `just upstream` read nothing (a dead network, a proxy), so a run that learned nothing uses up the week. Touch it only when at least one source was read. It matters on a machine that keeps its .git; a sandbox starts fresh anyway. — done in 3.23.0 (spec 013): session-start now writes the weekly stamp only when `just upstream` ran and did not end in upstream.py's "Nothing could be read". A run that could not run at all still says "upstream: could not run" and is also asked again at the next session. upstream.py is untouched: the decision is read from the sentence it prints today.
- [x] 2026-09-19, ai-backbone: when the 5,000 KB check-added-large-files hook blocks a save, the not-saved message gives a non-programmer no way out, and because save stages everything, one big asset blocks every save until somebody takes it out. The message should name the file and say what an agent does next. Do not raise the limit before a real asset has been measured (a guessed 15,000 KB was rejected in review on 2026-09-19). — blocked: it changes a message of `just save`, so an attended session builds it — done in 3.23.0 (spec 013): The refused save names each file with its size and the limit (read from the hook's own line: prek 0.5.3 measured, pre-commit's read in its source on this machine), tells the person nothing is lost and that it is the agent's job, and tells the agent the ways out in order. The limit is not raised. One thing beyond the message: the named files are taken back out of what is staged, one path at a time, because git keeps tracking a staged file after it is ignored and the agent that ignored it was refused again (measured); a whole-index reset was avoided because it also ends a merge in progress (measured on git 2.55.0). Turkish and English through _msg; the sentences are in person_reads for the maintainer.
- [x] 2026-09-19, ai-backbone: goes with the Flutter language layer when it is lifted from the first real Flutter project, measured there on 2026-09-19: `outdated` is `flutter pub outdated --no-transitive` plus a line for locked-but-movable packages, because alone it prints a false all-clear; no test-fast recipe, `flutter test` took one to four seconds; keep `flutter analyze`, `dart analyze` exits 0 on infos; examples/README.md and the ci-check comment say Flutter was checked and only the concurrency rule applies. And a not-yet list in the layer's header, each item tied to the event that will measure it: release signing, store upload, a build-number bump, flavors, device integration tests, golden tests, the Dart SDK's own MCP server (it runs the app and changes files, so it needs a permission decision first), a toolchain line in doctor (that one changes a command the person types). — blocked: the first real Flutter project's first spec has not passed its acceptance list (it was a draft on 2026-09-21). What did not depend on the lift went in with 3.23.0: Dart counts as code, the seed files, the update bot, the audit lens, the code map. — done in 3.25.0 (spec 015): outdated is `flutter pub outdated --no-transitive` plus pub's own dry-run answer, and the all-clear only on pub's own phrase; no test-fast; flutter analyze; examples/README.md and the ci-check comment say Flutter was checked and which rules apply; the not-yet list in the layer's header, each item tied to its event (the Dart SDK's MCP server and the doctor toolchain line among them).
- [x] 2026-09-19, ai-backbone: templates/routine-project.md still says "paste it as the prompt" and pushes with `git push origin HEAD`, which a checkout with no branch refuses; routine.md learned both lessons in 3.21.0. Point the agent at the file instead, push to the default branch by name, and carry over the sentence for a refused push. — done in 3.23.0 (spec 013): The brief now says a scheduler elsewhere is pointed at the file, never given a pasted copy, and offers the three sentences its stored prompt needs (the file that sh .ai-backbone/routine.sh --brief names; missing or not beginning "# Routine": change nothing). Push: less than the line asks, on purpose. git push origin HEAD stays for a checkout on a branch, because that is a person's own machine, where .claude/settings.json allows exactly that push and where HEAD:main from a feature branch would put that branch's work on main; with no branch (measured on git 2.55.0: "not a full refname") it pushes to the default branch by name, found with git ls-remote --symref origin HEAD. The refused-push sentence is carried over (pull --rebase, checks again when more than docs/ came in, push once more; rebase --abort and say so). It stays a project brief: one task of the oldest …
- [x] 2026-09-19, ai-backbone: idea: Claude Code 2.1.277 is reported to read AGENTS.md when a project has no CLAUDE.md -> the CLAUDE.md pointer this backbone puts into every project may be removable (CLAUDE.md, the seed list in .ai-backbone/manifest.txt, sync-rules in core.just). Read the changelog line itself before building: https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md . A removal only if every Claude Code a project might run reads AGENTS.md; otherwise close this line with the reason. — closed without building (spec 013): closed 2026-09-19, the pointer stays. The changelog line itself (raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md, under `## 2.1.277`, read 2026-09-19) says: "Added AGENTS.md support: in a project with no CLAUDE.md, Claude Code reads AGENTS.md instead; change it under "Project instructions" in `/config` (not yet on Bedrock, Vertex or Foundry)". Claude Code's memory page (code.claude.com/docs/en/memory, section "When AGENTS.md support is unavailable", read the same day) lists the sessions that still read CLAUDE.md only: every version before 2.1.277 (this Mac ran 2.1.273, measured; 2.1.277 came out on 2026-09-18 per npm), a session that fetches no feature flags (telemetry off, DO_NOT_TRACK, a third-party provider, a gateway), the first session after an install or upgrade, and `disableAllHooks`. The scheduled agent starts on a fresh machine every run; …
- [x] 2026-09-19, ai-backbone: idea: GitHub's docs are reported to say Copilot reads AGENTS.md anywhere in a repository, and Junie's docs to read .junie/AGENTS.md and then the root AGENTS.md, calling .junie/guidelines.md the legacy format -> the full copies sync-rules generates for those two tools may be duplicate context, or a legacy path (sync-rules in core.just, AGENTS.md section 5). Copilot: https://raw.githubusercontent.com/github/docs/main/data/reusables/copilot/custom-instructions-agents.md . Junie's page is closed in the sandbox: needs a local session. — closed without building (spec 013): closed 2026-09-19, no recipe changes; both vendors' pages were read first-hand that day (raw files and raw HTML with curl, no summarising fetch). Copilot: the reusable does say AGENTS.md may sit "anywhere within the repository", but GitHub's support table (github/docs, content/copilot/reference/custom-instructions-support.md) gives agent instructions to the cloud agent, the CLI, VS Code chat and code review on github.com only. Copilot Chat on github.com, in Visual Studio, JetBrains, Xcode and Eclipse, and code review inside the IDEs, read `.github/copilot-instructions.md` and not AGENTS.md; GitHub's own words are "currently not supported by all Copilot features". So the copy is the one file every Copilot surface reads, not a legacy path, and Zed still stops at it before it reaches AGENTS.md ("The first matching file is used", zed docs/src/ai/rules.md). Where both are …
- [x] 2026-09-19, ai-backbone: idea: session-start prints the journal, the commits, the backlog and upstream, but never the specs that are open -> one line per draft or approved spec: name, status, n of m tasks ticked, the first open task; anything that is not [x] counts as open (session-start in core.just). Borrowed from the Next line of OpenSpec's status command: https://raw.githubusercontent.com/Fission-AI/OpenSpec/main/CHANGELOG.md — done in 3.23.0 (spec 013): In every project, session-start prints one line per draft or approved spec, between the working tree and the code map. The line gives the name, the status, n of m boxes under "## Tasks" ticked (only [x] counts), and the first line of the next open task, with "…" when the task runs on. A draft says "no code until approved". A spec whose tasks are all ticked says to walk the acceptance list and close it. Nothing is printed when no spec is open or the project has no docs/specs.
- [x] 2026-09-19, ai-backbone: idea: a question put to the maintainer carries the answer you would give and one plain sentence on why it matters, so that yes is an answer -> two sentences in step 3 of .agents/skills/spec/SKILL.md. Borrowed from Spec Kit's clarify step, which asks at most five questions, each with a recommended answer. Do not read its command template unattended: it is a prompt. — done in 3.23.0 (spec 013): Two sentences in step 3 of .agents/skills/spec/SKILL.md ("A question carries the answer you would give and one plain sentence on why it matters to them, so that "yes" is an answer. A bare question hands a product owner the engineer's homework."), a clause in the Questions section of templates/spec.md, and one in section 6 of the walkthrough. Spec Kit's command template was not read.
- [x] 2026-09-19, ai-backbone: Decide: does ai-backbone become a public template repository? README's Start step 1 (Download ZIP) works only for the owner today. Before any yes, an attended session scans the tree and every commit for project names, repository slugs other than this one, run and job ids, machine paths and e-mail shapes; chooses between a fresh one-commit history and the old one; stops backbone-note writing project names into this file and its commit subjects; makes a new project's language default neutral without changing what the current maintainer gets; and checks that the scheduled agent can still push to main. I would answer: not yet. — closed 2026-09-23 by the maintainer: they will decide when the time comes, and it needs no note here meanwhile. → spec 017 decided (2026-09-24): public if the gate's minutes run past the free 2,000 a month, as its own spec after this clean-up and an audit.
- [x] 2026-09-19, ai-backbone: Decide: GitHub Actions is switched off on this repository, so nothing outside the scheduled agent checks what it pushes to main, while docs/01-getting-started.md section 5b and CHANGELOG 3.16.0 describe a run on every push. Switch it back on, or have the docs say that the agent's own self-test is the only check before a push. I would answer: switch it back on when you can; until then the docs say so. — closed 2026-09-23 by the maintainer: they will decide when the time comes, and it needs no note here meanwhile. → asked and answered in spec 017 (2026-09-24): switched on, for the backbone, once the gate's workflows are in place.
- [x] 2026-09-19, ai-backbone: every scheduled run in a sandbox starts with a red suite: prek builds the gitleaks hook with Go and fetches a Go from go.dev, which is closed there, so the first `just self-test` fails about ninety checks until the agent applies the workaround in routine.md's quirks by hand (GOTOOLCHAIN through the module proxy, then `prek prepare-hooks`). Seen in the run of 2026-09-19 13:03 UTC: three minutes, a baseline run on a stash, and one denied command before it found the cause. Let `just hooks-install` do it: when `prek prepare-hooks` fails, `go` exists and go.dev does not answer, fetch the toolchain prek asked for through GOTOOLCHAIN, put it first on the PATH for that call, and try once more; say so in one line. Then the quirk leaves the brief. — done in 3.23.0 (spec 013): hooks-install now runs `prek prepare-hooks` after installing the shims, with a log file of its own. When that fails, the log names a Go that prek went to go.dev for, and a go is on the PATH, it runs `GOTOOLCHAIN=go<version> go env GOROOT` to fetch the toolchain through the module proxy. It then retries once with that toolchain first on the PATH and says so in one line. When the retry fails too, it prints prek's own words and still exits 0, so the first save tries again. It prepares nothing in offline mode. I rehearsed it twice with real tools on this Mac (prek 0.5.3, a real go 1.23.0 on the PATH, go.dev closed by a dead proxy, an empty PREK_HOME). gitleaks was built through the fetched Go 1.27.1, and a later save passed with go.dev still closed. The suite tests the logic with a stand-in prek and a stand-in go. I could not reach the sandbox; the routine.md sentence is …
- [x] 2026-09-19, Swap Smash: AGENTS.md §7 should say it outright: an agent never puts the maintainer's name or email into a request to a third-party service, a User-Agent header included. A research agent working for Swap Smash put the owner's email into the User-Agent of three crates.io API calls, because crates.io asks for contact details; an anonymous User-Agent, or asking first, was the right move. AGENTS.md is a seed, so the CHANGELOG entry must name it. — done in 3.23.0 (spec 013): One bullet at the end of section 7 of the seed AGENTS.md: "- Never send the maintainer's name or e-mail to a third-party service, a `User-Agent` header included. Stay anonymous, or ask first." AGENTS.md is a seed, so the CHANGELOG paragraph names it and gives the line for existing projects to add by hand.
- [x] 2026-09-19, ai-backbone: from the maintainer: a picture of how the backbone works, for a person who does not read code. One page, docs/how-it-works.md, with a flowchart GitHub draws by itself (a mermaid block in Markdown: no tool, no image file to keep current). What it shows: a project's agent leaves a note (just backbone-note) -> the backbone folder next to the projects -> GitHub -> the scheduled agent, one item a day and the radar on Sunday -> main -> the folder takes it at the next project session and tags the version -> a project takes it with just template-update. Beside it the two side loops: a `- [?]` question reaches the maintainer in Sunday's report and the answer comes back as a decision note; and brain/ never leaves the machine. Labels short enough to follow without English. A second, smaller picture for the life of one idea: note or idea -> spec -> build -> self-test -> version -> projects. Link it from README.md and docs/README.md. — blocked: the maintainer wants to shape this together, in an attended session — done in 3.23.0 (spec 013): docs/how-it-works.md: two mermaid flowcharts (TD) and a paragraph under each that says the same in words. The first, 12 boxes: Your project, A note, Backbone folder, GitHub, The to-do list, News from other tools (Sunday: the radar), Scheduled agent, New version and back to the folder and the project; beside it A question for you, You, yes or no back into the project; and a frame "Never goes to GitHub" with brain/ notes and Second copy. The second, 6 boxes: A note or an idea, One-page spec, The agent builds, just self-test (red goes back, green goes on), New version, Your projects. Every label quoted, no HTML, themes, styles or click handlers. Both parsed and drawn with mermaid-cli 11.17.0 through npx, with a scratch npm cache and the Chrome already on the machine, nothing installed globally; a deliberately broken diagram was refused by the same command. Linked from …
- [x] 2026-09-19, ai-backbone: from the maintainer: say which public standards a project built on this follows, so that the next project and the existing ones do the same without being told. Today only the backbone is told, about itself: semver.org is named in core.just's header and in CHANGELOG.md, and a project is told nothing. Candidates: semver.org for version numbers, keepachangelog.com for a CHANGELOG, conventionalcommits.org for commit messages (just save already enforces the prefix and names no source). One short paragraph with the addresses, in a skill rather than in AGENTS.md: a skill reaches existing projects through just template-update and the seed AGENTS.md does not, and AGENTS.md has about 350 bytes left under its 7,000-byte ceiling. To settle with the maintainer: which standards make the list, and whether a project's first version is 0.1.0. — blocked: the maintainer wants to do this together, in an attended session — done in 3.23.0 (spec 013): One paragraph, "Public standards", in the session skill (and a clause in its description so an agent finds it): https://semver.org for version numbers, first version 0.1.0 and 1.0.0 the first one other people rely on; https://keepachangelog.com for a CHANGELOG.md in the project's root from its first version on; https://www.conventionalcommits.org for commit messages, with the eleven kinds the save recipe and the commit hook accept (feat fix docs chore test refactor style perf build ci revert) and that just save writes chore: for a message that names none. A check reads the kinds out of core.just's save recipe, so a kind added there and not in the skill goes red. Needs one line in _root-strays that I do not own: today just doctor calls a project's root CHANGELOG.md a stray (measured); see needs_elsewhere.
- [x] 2026-09-19, ai-backbone: from the maintainer: brain/ is ignored by git on purpose, so in every project built on this it exists on one disk only, and nothing offers a second copy. To talk through before building: a zip of brain/ into a folder the person names once (a cloud drive folder, a second disk), written by session-end only when brain/ has changed since the last one; or brain/ as its own private repository with its own address; or the two together. Known against the second: what is kept off GitHub on purpose would go to GitHub after all. Reported, not measured here: a git repository placed inside a synced cloud folder gets damaged by the sync, so a zip is the form that belongs in such a folder. Whatever is chosen: no new tool, one question asked once, and doctor says so while there is no second copy. — blocked: the maintainer wants to talk it through first — done in 3.23.0 (spec 013): done, as a mirror and not a zip or a private repository (the reasons are on the line below): no new tool (rsync is on every Mac), one question asked once per machine and kept in git's global settings (ai-backbone.vault-copy; an agent sets it, no recipe does), and doctor says in its Repo half while there is no second copy, without calling it a fault. With no place set session-end says one line and copies nothing; a place that is not there, cannot be written, or a machine without rsync is said in plain words and the session still ends.
- [x] 2026-09-19, ai-backbone: from the maintainer: a project that has no GitHub address has no second copy of its code, and after the first day nothing says so: just publish offers to create the repository, but only when somebody types it, and just projects shows the missing address only to somebody who opens the backbone. Let a project's session-start say it in one line for as long as it is true (no GitHub address yet -> just publish), so that the agent working there brings it up with the person. — blocked: the wording is for the maintainer to see first, together with the line above about brain/ — done in 3.23.0 (spec 013): session-start prints one line at every session for as long as `git remote get-url origin` fails, in offline mode too, and in the backbone itself only when it has no address. The exact sentence is in person_reads for the maintainer to see. It tells the agent to ask the person before `just publish`, because publishing creates a repository under their account. The line is English like the rest of session-start, and `_msg` is not mine, so there is no Turkish form in the code.
- [x] 2026-09-19, ai-backbone: from the maintainer, about the second copy of brain/ (the line above): one copy per project, under the project's own name, never a growing pile of files, and what is in the cloud after a session has ended is the newest state. Measured on the maintainer's machine on 2026-09-19: eight vaults from under 1 MB to about 90 MB, most of it a few large files that never change, while the journal changes in every session, so a zip would be rewritten and uploaded whole each time. That argues for a mirror instead of a zip: session-end copies brain/ into <folder>/<project>/ with rsync (on every Mac already), without deleting there what was deleted here, so the drive uploads only what changed, there is exactly one folder per project, and an accidental deletion is not copied. No vault holds a git repository (measured), which is the thing a synced folder is reported to damage. The folder is asked for once per machine, not once per project. Whether the copy has reached the cloud is the drive's business and no recipe can promise it: session-end can only say the copy was written, and doctor can say how old the last one is. Also seen: two projects keep an .env outside brain/, which no copy of brain/ would hold; AGENTS.md section 7 already says where keys belong. — blocked: the maintainer wants to talk it through first — done in 3.23.0 (spec 013): done: session-end, after the journal step, mirrors brain/ into <place>/<project>/brain/ with rsync -a and no --delete (flags measured on openrsync protocol 29 on macOS 27 and on rsync 3.4.1 built for the measurement); <project> is the project row of AGENTS.md section 0 through _slug, and a project whose row still says ai-backbone (adopt copies the seed) is filed under its folder name. The secret files git ignores (.env, .env.* but never .env.example, .envrc, *.pem *.key *.p8 *.p12 *.pfx *.jks *.keystore *.mobileprovision; outside brain/, .references/ and node_modules target build dist .venv Pods DerivedData .dart_tool .gradle) are copied beside it under secrets/ at their own paths, never moved. A .git inside brain/ is left out and said. A place inside a folder git saves is refused, because the secrets would land in that project's next save (this is how the Desktop is …
- [x] 2026-09-21, Social Swap: Multi-agent research prompts need a 'no Gradle/Android builds' line: a read-only research agent's trial build made Gradle auto-install Android SDK Platform 34 (126 MB) into the global SDK without asking, against AGENTS.md section 7 — done in 3.23.1: the `audit` skill is the one place the backbone spawns many read-only agents, and its rule said only "never edit a file", which reads a build as a read. The rule is now "never edit, and never build", with the measured 126 MB on it and the general case named (every toolchain that fetches what it is missing: Flutter, Xcode, `cargo`), and step 2 has the ground rules block name the commands the reviewers may run. Whether the app builds stays step 7, the rehearsal, where a person is watching.
- [x] 2026-09-22, ai-backbone: `.gitignore` does not ignore `__pycache__/`, so a `just save` right after `just upstream` commits `.ai-backbone/__pycache__/upstream.cpython-311.pyc`. Measured today: the scheduled run staged it, and a tracked `.pyc` is worse than clutter — it embeds the source's mtime, so a fresh checkout recompiles it, `git status --short` is never empty again, and step 0 of the routine brief stops every future run before it starts. The run kept it out with `.git/info/exclude`, which is local and gone with the sandbox. The fix is one line in the stack section of `.gitignore`, where Python's `__pycache__/` sits as a commented example; `.gitignore` is a `[seed]` file, so no update carries it into a project that already exists, and every project gets a copy of `upstream.py` and `radar.py` and so the same hole. — done in 3.23.2 (an attended session, the same day): `__pycache__/` and `*.pyc` are ignored in the seed `.gitignore`, in their own section that says why; a check plants a .pyc in a new project and fails if a save would take it. A project that already exists adds the two lines itself, and the CHANGELOG entry says so.
- [x] 2026-09-22, ai-backbone: idea: the three Python helpers (upstream.py, radar.py, budget.py) could become one Rust binary with no runtime, the way just and prek are -> setup.sh, manifest.txt, the _py recipe. Only if the code map (graphify, a Python tool) ever leaves the backbone: until then Python stays through uv, and a binary per OS and architecture with a release pipeline would add more than it removes (the maintainer weighed it on 2026-09-22). — blocked: graphify leaving the backbone — re-checked 2026-09-23 (spec 015): still blocked. graphify is still installed by setup.sh and listed in manifest.txt. — closed 2026-09-23: an idea that waits on graphify leaving the backbone, which is the maintainer's call and not an open task. The reasoning stays on this line.
- [x] 2026-09-22, ai-backbone: budget.py reads only Claude Code's transcripts and meter. Codex CLI records per-turn usage AND its remaining percent in $CODEX_HOME/sessions/*/rollout-*.jsonl (event_msg token_count with rate_limits.used_percent), and Gemini CLI records tokens per message under ~/.gemini/tmp/<sha256 of the project root>/chats/ (read 2026-09-22). A reader for each is about forty lines; the line at session start and the cap are the same for every tool. — blocked: a project on this backbone that uses Codex or Gemini — re-checked 2026-09-23 (spec 015): still blocked. No ~/.codex and no Gemini CLI project under ~/.gemini/tmp on the maintainer's machine; the Antigravity IDE keeps data under ~/.gemini, but that is an IDE, not the Gemini CLI this reader is for, and the agent running in it is Claude Code, which budget.py already reads. — closed 2026-09-23: it waits for a project on this backbone to use Codex or Gemini, which is the maintainer's choice of tool. Where each keeps its usage stays on this line for that day.
- [x] 2026-09-22, Smash: adopt leaves | project | ai-backbone | in AGENTS.md section 0 and takes no name or languages, while new-project takes them; measured adopting ~/Desktop/games on 2026-09-22, the agent edited section 0 by hand (project, stack, source_dir) — done in 3.24.1: `adopt` now writes the folder's own name into the project row (reproduced first: a throwaway repo adopted in this sandbox came out saying `| project | ai-backbone |`), prints one line saying so, and a self-test check fails without it. The name is the only thing the recipe is given; `brain_lang` and `chat_lang` stay as they are, because AGENTS.md section 0 says those are the maintainer's answer and `new-project` leaves them alone too when no language is typed. An argument for them would change what a person types, so it was not added. `stack` and `source_dir` are the agent's to fill when the time comes, by section 0's own wording.
- [x] 2026-09-22, Smash: idea: ref-add takes only a git URL. A folder that is an old repo of the maintainer's own with no remote (three earlier games, 1.4 GB, moved into games/.references/ by hand on 2026-09-22) has no recipe; ref-fetch rightly skips an entry whose url is empty, but the references README does not say a local-only entry is allowed — done in 3.25.0 (spec 015): `ref-add <folder>` clones a git repository in (listed with its address when it has one, user and password stripped, else url = "" and a `from` line), lists a folder already in .references/ where it stands, and refuses a loose folder; `path` keeps its old meaning, where a reference lives and is read in place; ref-fetch says which local-only ones are there, and how to bring one back when the list has a restore line.
- [x] 2026-09-22, Smash: Smash: idea: 'just stale', a docs-drift check for 'just lint': every backticked repo path in docs/, README*.md, AGENTS.md and src/*/README.md that no longer exists in the tree, plus every old name in a docs/renames.toml the agent appends to at each rename (old, new, date); print file:line. Measured need 2026-09-22: two folder renames and one layout change left 51 stale lines in 18 files, found only by a hand grep. An amended ADR keeps its old names on purpose and carries 'Status: accepted; amended by NNNN' at the top, so the check skips files whose Status line says amended. — done in 3.25.0 (spec 015): `just stale` reports backticked paths git once held that are gone, and old names in docs/renames.toml, as file:line, skipping an amended ADR and what git ignores; lint runs it and never fails on it; quiet on the backbone's own docs. Honestly measured: on Smash's tree just before its hand fix it finds 1 of the 51 lines — the rest named a layout that was planned and never committed, which only a renames.toml entry catches, and the session skill now says to record one.
- [x] 2026-09-22, Smash: Smash: idea: 'just adr <name> --amends NNNN' writes the new ADR and adds the pointer line 'Status: accepted; amended by <new> on <date>: <one line>' to the old one; today it was done by hand (ADR 0016 amending 0002 and 0007), and the architecture page's rule that an amended ADR gets one pointer line is otherwise easy to forget. — done in 3.25.0 (spec 015): writes the new ADR with its Relates line and puts the pointer on the old one's Status line ("and by" for a second), passing over a YAML front matter and finding a Status line anywhere in the file.
- [x] 2026-09-22, Smash: Smash: stack-flutter.just must learn pub workspaces: app folders apart from the workspace root (ws_dir, members), get and analyze at the root, dart test for the pure packages, flutter test per app; Smash's stack.just of 2026-09-22 is the draft to lift. — done in 3.25.0 (spec 015): ws_dir and app, get and analyze at the workspace root, dart test for pure packages and flutter test per app, members read from `workspace:` in every spelling pub accepts; members named in `pure` always run dart test and stop lint and test the day they take Flutter in.
- [x] 2026-09-22, Smash: Smash: .github/workflows/checks.yml of 2026-09-22 is a candidate for examples/ci-flutter.yml: flutter-action pinned by commit with flutter-version-file at the workspace root, setup-just, paths-ignore docs, 10-minute cap, the backbone's concurrency lines. Today 'just ci-init flutter' writes dependabot then exits 1, which breaks a script that chains it. — done in 3.25.0 (spec 015): ci-flutter.yml with flutter-action pinned by commit, flutter-version-file, setup-just, docs-only pushes skipped, a ten-minute cap and the concurrency lines; `just ci-init flutter` exits 0, points the workflow and the update bot at the real pubspec.yaml, and asks which language when a project has two. Never run on a GitHub runner: Actions is off on the source project too.
- [x] 2026-09-23, Smash: Smash: hooks-install installs pre-commit and commit-msg only; a project whose CI is off (free GitHub, Actions disabled) wants 'just test' before every push. Idea: the seed pre-commit-config gets a commented pre-push 'just test' hook and hooks-install adds -t pre-push when the config has a pre-push stage (Smash did this by hand on 2026-09-23, ADR 0018: just test 15 s, runs on every save-push). — done in 3.25.0 (spec 015): the seed config carries a commented pre-push hook running `just _test-if-any` (quiet until the project has a test recipe), and hooks-install and template-update install the pre-push stage whenever the config has one; doctor says when the config has one and git will not run it.
- [x] 2026-09-23, Yerly XL: Rust projects on one machine share one CARGO_TARGET_DIR (~/.cargo-target, set in .zshrc to keep build output off the synced Desktop), so two IDE windows building two projects wait on each other's cargo lock, and two heavy builds at once strain a 24 GB machine (a Force Quit on 2026-09-19). Proposal for the Rust layer: (1) the recipes build into ~/.cargo-target/<project> rather than the shared root, which ends the lock wait; sccache (RUSTC_WRAPPER, already on) keeps compiled crates shared, so little is lost; (2) heavy recipes (test, lint, e2e, save's checks) first look for a cargo or rustc process belonging to another project and, if there is one, print one line and wait for it to finish (with a time limit) instead of starting a second heavy build. Agents should do the same before a long command. — done in 3.25.0 (spec 015). The waiting as proposed: the Rust layer's heavy recipes (check, test, test-fast, lint, build) start cargo through the core's `_build-wait`, which reads this person's own programs with POSIX `ps -u <uid> -o stat= -o comm=` (falling back to `ps -A -o comm=`), skips one stopped with Ctrl-Z, prints one line when it starts waiting and one when it goes on, and after BUILD_WAIT_MINUTES (20) goes on with half the cores; where ps cannot list it says nothing and does not wait; BUILD_WAIT_MINUTES=0 switches it off; `just save` waits where the person can see it rather than inside the checks; the session skill tells agents to wait before a long command. The folder part by another road: two of this line's premises were wrong when measured — the Desktop it came from is not synced (FXICloudDriveDesktop is 0) and sccache's 0 hits were counted while everything was already built (after a clean, 1348 of 1934 compile requests hit) — and a folder chosen inside `just` alone would split the IDE's own build from the recipes', so the recipes never move it. doctor says when a build folder is outside the project, what that costs and the line that ends it. On the maintainer's machine the line was removed from .zshrc on 2026-09-23 with their word, and the old 334 GB folder deleted.
- [x] 2026-09-23, Yerly XL: Correction to the shared CARGO_TARGET_DIR note of 2026-09-23, measured the same day on the maintainer's machine (Apple M5 Pro, 15 cores: 5 performance + 10 efficiency, 24 GB): sccache has 0 hits out of 412 misses, so it does not share compiled crates between projects today and a per-project build folder would cost each project its own dependency build. The shared folder has also grown to 335 GB, 322 GB of it debug output, with 280 GB of disk left. So the Rust layer wants three things rather than two: a per-project folder under ~/.cargo-target so one project can be cleaned without the others; a cap on parallel jobs (CARGO_BUILD_JOBS about 5) when another project is building, since memory rather than CPU is what three simultaneous builds run out of, at link time; and a size check that says when a project's build folder has grown past a limit and offers the one command that clears it. — done in 3.25.0 (spec 015). The folder is cargo's own answer (`cargo metadata`, so a `[build] target-dir` in any .cargo/config.toml counts), doctor measures it (`du -H -s -k`, bounded by BUILD_MEASURE_SECONDS, 60) and says when it is over BUILD_OUTPUT_WARN_GB (50) or too big to measure, and `just clean-build` says its size, asks, clears exactly the folder it measured, and refuses one outside the project. The cap is CARGO_BUILD_JOBS at half the cores when the wait gives up. The debug-info advice gives what was measured on Yerly XL's full test build on 2026-09-23: 9.8 GB with debug = 1 (the machine's ~/.cargo/config.toml set it for every project, which was not seen at first and is corrected in 3.25.2), 8.6 GB with line-tables-only, about a tenth; full debug info, the default, was not measured and is larger. What fills a disk is builds nobody clears, not the debug map.
- [ ] 2026-09-23, Smash: Smash: a dev-dependency CLI (patrol_cli 4.8) wrote shell completion into ~/.zshrc and ~/.dart-cli-completion on its first run (cli_completion auto-install); undone. Tools built on cli_completion want their opt-out env (PATROL_NO_COMPLETION) in every recipe that runs them; doctor could warn when ~/.zshrc gains a '## [Completion]' block. — checked 2026-09-24: no recipe in the backbone runs a cli_completion tool, so there is no env to set here; what the backbone owed was the rule, and 3.26.1 puts it in the backbone-dev skill beside the `$1` rule, for every recipe an agent writes in a project. The rest is not an unattended run's: naming patrol_cli's opt-out variable would be writing an unmeasured fact into a template (AGENTS.md §8), and the doctor warning changes a message the person reads. blocked: needs an attended session on a machine with Flutter — measure the opt-out variable, then decide the doctor line.
- [ ] 2026-09-24, ai-backbone: a fresh clone of the backbone shows no brain/ until the first just session-start runs brain-init (the template ships in .ai-backbone/brain-template/); with the repository public a newcomer sees none and README's 'What you see' does not say so — spec 021 (the brain's logic as examples in the repo) should ship the skeleton visibly or the README should say when it appears
- [x] 2026-09-24, ai-backbone: Windows, from the gate's first runs (spec 017, run 35980796058): setup.sh and every tool work under Git Bash and the suite runs, but 223 checks are red and it is cut by the cap in the build-output section. First lead: 'AGENTS.md at most 7000 bytes (got 7122)' and the journal-size check fail by exactly a CRLF difference, so the runner's git checks the files out with CRLF (core.autocrlf); one line in .gitattributes, '* text=auto eol=lf', may turn most of it green. — done in 3.27.3 and 3.27.5 (spec 020): the backbone and every new project check out with LF; the size checks are green on the gate (AGENTS.md 6984 bytes on Windows).
- [x] 2026-09-24, ai-backbone: Windows, the red by section at 20 minutes (run 35980796058, 196 ok): lint before save 57, the budget 51, the clone keeps itself level 26, the gate 15, what the language needs 16, save/ci/template-update 16, the radar 12, setup.sh 10, build output 8, checks on every push 5, new-project 3 ('new-project runs' itself). Read after the CRLF line above; what stays red is MSYS: ln -s, ps -o, python3, file:// remotes, paths. — done in 3.27.3 to 3.27.6 (spec 020): run 35993791440 has 3 red where it is cut, from 220; the causes were CRLF, Python's cp1252 and CRLF output, the suite's git identity given as /c/ paths, links, and paths inside Python and URLs. The three left are the next line.
- [x] 2026-09-24, ai-backbone: Windows, the suite does not end in 20 minutes: two checks in the build-output section wait on ps and a measuring timeout ('a folder too big to measure in time says so, and in time'), and the ten minutes after the tenth bought six checks; the suite step is cut at eight minutes on Windows so the job ends as a failure the run tolerates. Until the suite ends there, the cap stays. — done in 3.27.8 to 3.27.10 (spec 020): the ps wait was never the cause; du's watcher waited its whole minute under Git Bash, and each refused request to 127.0.0.1 cost two seconds. The whole suite ends in 15 minutes there (run 36010227211).
- [x] 2026-09-24, ai-backbone: Windows, what is left after 3.27.6 (run 35993791440, spec 020): the suite is cut at eight minutes in the upstream section, where each request to the port nothing listens on costs about two seconds on Windows (one check took 52 s); three checks are red before that: 'a version bumped in unsaved work is not tagged on the commit before it', 'hooks-install fetches the Go prek could not', 'upstream runs where python3 has no tomllib'. Making Windows decide needs the suite inside its cap, then a change to checks.yml that only an attended session carries to main. Also open: `just snapshot` needs zip, which Git Bash lacks; and a real Windows machine with Claude Code has not been tried. — done in 3.27.7 to 3.27.10 (spec 020): Go's C:\ folder split the PATH, Git Bash turned origin/main:x into origin\main;x so no tag was ever made from Windows, the Rust layer took C: paths for relative ones, and the rest were the suite's own paths and quoting. Run 36010227211: 522 green, 0 red on Windows. The two open parts are the next line.
- [ ] 2026-09-24, ai-backbone: Windows after spec 020's step 4: `just snapshot` needs zip, which Git Bash lacks (the suite skips its check there); the gate still reads Windows as report-only (step 5: gate.sh and its checks, then checks.yml, which an attended session carries to main); and Claude Code on a real Windows machine has not been tried: the session line and the project's skills. blocked: step 5 waits on the maintainer's word, the last on a Windows machine
