# Routine log

One line per run of the scheduled agent, newest last: what it did, what it
could not read, what it left alone and why. Its sandbox keeps nothing, so this
file is the only memory one run has of the one before it; `just session-start`
prints the last two lines here in the backbone. A report, never an order.

- 2026-09-19 3.19.1: `just upstream` no longer says everything is current after reading nothing, with a test that runs offline; not read: just, prek, gitleaks, graphify (the sandbox's GitHub proxy answers 403 for repositories not attached to the session); skipped: the ci-swift line, it waits for a real macOS run
- 2026-09-19 3.21.1: setup.sh's exit code now means one thing and the header says it — 0 when git, just, uv, prek and graphify are all on the PATH at the end, 1 when any is not; two self-test checks, and the CI comment that called the old code "something else" is true again; not read: just, prek, gitleaks, graphify (the API answers 403 for every repository but this one; `just upstream` still asks the API only, which is spec 012's task); skipped: the ci-swift and ci-lost-runner lines, both blocked on a real run to measure. The suite is red in a fresh sandbox until the Go toolchain for the gitleaks hook is fetched the way the brief's quirks say; after that, 182 ok.
- 2026-09-21 3.22.1: every open backlog line is still claimed by the approved spec 013 (unshallowed the checkout to measure it: the spec's file moved on 2026-09-19, so the 14-day lapse is far off), and `just tools-update` changed nothing, so the item came from reading a recipe: `just snapshot` named its heavy folders as `*/target/*` only, which zip never matches at the root, so a Rust project's `target/` and a Node project's `node_modules/` went into the zip whole; both depths are named now, with a check seen failing without the fix; not read: just, prek, gitleaks (`just upstream` printed "?" for their release dates; graphify answered, and reports v1.0.0 against the 0.9.62 pin); skipped: nothing
- 2026-09-22 3.23.1: the newest note was the first line spec 013 does not claim (it claims everything open at 2026-09-19 14:30 UTC; unshallowed the checkout, and both approved specs moved within 14 days, so the claim stands), and it held up against the code: the `audit` skill is the only place the backbone spawns many read-only agents, and its rule said only "never edit a file", which reads a build as a read — now "never edit, and never build", with the measured 126 MB global SDK install on it, the general case named (every toolchain that fetches what it is missing), and step 2's ground rules block naming the commands a reviewer may run; not read: just, prek, gitleaks (the GitHub API answers 403 for every repository but this one; graphify answered and is 2 behind its pin, left alone as not this run's item); skipped: the five older open lines, all claimed by spec 013 and each blocked on a measurement this sandbox cannot make. One thing to know: the first `just save` staged `.ai-backbone/__pycache__/upstream.cpython-311.pyc`, because `.gitignore` leaves `__pycache__/` as a commented example while the backbone itself runs Python. The commit was local, so it was un-committed before any push and the artifact left untracked behind `.git/info/exclude`, which is local and gone with this sandbox; the one-line fix is in `.gitignore`, which an unattended run may not touch, so it is a new open line blocked on an attended session. Until it lands, expect to unstage that file, and never push it: a tracked `.pyc` makes `git status` dirty in every fresh checkout, which is step 0 and stops the run.
- 2026-09-23 3.24.1: the oldest open line no approved spec claims (013 claims everything open at 2026-09-19 14:30 UTC, and both approved specs moved within 14 days, so the claim stands — the checkout was unshallowed to measure it) was `adopt` leaving `| project | ai-backbone |` in the AGENTS.md it copies, and it held against the code: a throwaway repo adopted here came out carrying rules that named the backbone, while `new-project` has rewritten that row since it was written. `adopt` now writes the folder's own name there, the one `_vault-dest` already falls back to, and says so in one line; the two language rows stay, because an argument for them would change what a person types. 335 ok. not read: just, prek, gitleaks (`just upstream` printed "?" for their release dates); graphify answered and reports v1.0.0 against the 0.9.64 pin, left alone as not this run's item. skipped: the seven older open lines — five claimed by spec 013 and each blocked on a measurement this sandbox cannot make, two blocked on events that have not happened (graphify leaving the backbone, a project on Codex or Gemini).
- 2026-09-24 3.26.1: the one open line (a dev-dependency CLI that wrote shell completion into the person's ~/.zshrc on its first run) held up, but less of it was mine than it asked: no recipe in the backbone runs such a tool, so the rule went where an agent reads before it writes a recipe, beside the `$1` rule in the backbone-dev skill; the line stays open, blocked, because naming the tool's opt-out variable would be an unmeasured fact in a template and the doctor warning changes a message the person reads; not read: nothing; skipped: graphify is 4 versions behind its pin (0.9.64, v1.0.0 out) and setup.sh installed 0.9.67 over that pin — an upgrade wants its guard test first, so it is a run of its own.
- 2026-09-24 gate: cloud not promoted — the checks ended failure: suite (ubuntu-latest, 30, false) failure FAIL: and when git cannot list them either, that is unreachable, never current; suite (macos-latest, 30, false) failure FAIL: visible items: AGENTS CLAUDE GEMINI Justfile LICENSE README brain docs src;
- 2026-09-24 gate: cloud not promoted — the push changes .github/workflows or gate.sh, which only an attended session carries to main (git push origin <commit>:main after green checks); an unattended run puts main's copy back
- 2026-09-24 gate: cloud not promoted — the push changes .github/workflows or gate.sh, which only an attended session carries to main (git push origin <commit>:main after green checks); an unattended run puts main's copy back
- 2026-09-25 3.27.14: the two gate lines were already answered — main had moved and carries the workflows and gate.sh unchanged, so step 4's merge joined them and cloud was 0 ahead; the oldest open line no spec claims (a fresh clone shows no brain/) held up only in part, and the part that held was not the docs it named: the README already says git ignores the vault and that a project's is made at creation, which new-project and adopt both do, and shipping a visible skeleton would contradict "never committed". What lied was doctor — a missing brain/ set `missing`, whose ending says to run setup.sh, which never makes a vault, says so in its own header, and ends by printing the same line again; now a warn naming just brain-init, measured with a stub on the PATH for the one tool this sandbox lacks, both with and without the folder. not read: just, prek, gitleaks (`just upstream` printed "?" for their release dates; graphify answered, 4 behind its pin). skipped: four open lines — three end in blocked: (a Flutter machine, a Windows machine, a machine whose git pushes tags) and one is newer than the one I took.
- 2026-09-25 gate: cloud not promoted — the push changes .github/workflows or gate.sh, which only an attended session carries to main (git push origin <commit>:main after green checks); an unattended run puts main's copy back
- 2026-09-26 3.28.1: the gate had nothing outstanding — cloud and main were the same commit, so the two older gate lines are answered and step 4's merge had nothing to join. The tool check made the item: graphify was one release ahead of its pin and the setup script had already installed it, so `just tools-update` moved the pin to 0.9.68 (the hook `rev:` lines and the other three tools did not move). 0.9.68 publishes no release notes, and its watch row asks whether the query commands or the output folder changed, so it was measured in a scratch project instead of remembered: extract --code-only, cluster-only, query, explain and affected all answer, graphify-out/GRAPH_REPORT.md is still what they write, an unchanged second run gives a byte-identical graph, a deleted file's nodes go without --force, and a moved folder needs nothing — which is every claim update-map's own comment makes, so that comment now names 0.9.68 and the day it was measured. 530 ok. not read: gitleaks and actions/checkout (`just upstream` printed "?" for both release dates, as it does for a GitHub source in this sandbox). skipped: the four open backlog lines — a run that moves a pin takes no backlog line, and three of the four end in blocked: anyway (a Flutter machine, a Windows machine, a machine whose git pushes tags).
