# docs

What this project is and how it works. English, public, kept current by the
agent: when code changes something described here, the page changes in the
same session.

| Folder | What goes in |
|---|---|
| `specs/` | One page per idea, written before the code. `just spec <name>`. Status: draft, approved, done or abandoned. |
| `adr/` | Decisions worth remembering, one file each, numbered from 0001. |

Anything private, half-formed, or in your own language belongs in `brain/`.
